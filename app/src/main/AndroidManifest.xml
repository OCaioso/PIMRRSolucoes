<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rrsolucoeshotel">
    <!-- permissões necessárias para conectar o banco de dados online -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RRSolucoesHotel">
        <activity
            android:name=".activities.TesteActivity"
            android:exported="true" />
        <activity
            android:name=".activities.FormGastos"
            android:exported="true" />
        <activity
            android:name=".activities.FormMenuServicos"
            android:exported="true" />
        <activity
            android:name=".activities.FormLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>